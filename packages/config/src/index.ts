import { z } from 'zod';
export const config = z.object({NODE_ENV:z.enum(['development','test','production']).default('development'),DATABASE_URL:z.string().url(),REDIS_URL:z.string().url(),JWT_SECRET:z.string().min(32),REFRESH_TOKEN_SECRET:z.string().min(32),APP_URL:z.string().url(),API_URL:z.string().url(),CORS_ORIGINS:z.string().default('http://localhost:5173'),PORT:z.coerce.number().int().positive().default(4000),MAX_PAGES_PER_AUDIT:z.coerce.number().int().positive().max(100).default(10),MAX_CRAWL_DEPTH:z.coerce.number().int().min(0).max(5).default(2),DEFAULT_MONTHLY_VISITORS:z.coerce.number().nonnegative().default(0),DEFAULT_CONVERSION_RATE:z.coerce.number().nonnegative().max(100).default(0),DEFAULT_AVERAGE_LEAD_VALUE:z.coerce.number().nonnegative().default(0)}).parse(process.env);
