# Development

Prerequisites: Node 20+, npm 10+, Docker Compose. Copy `.env.example`, install dependencies, start Compose, generate Prisma, migrate, seed, then run `npm run dev`. Check with `npm run typecheck && npm run lint && npm test && npm run build`.
