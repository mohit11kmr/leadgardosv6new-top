# Scoring

`calculateScores` is isolated from UI and scanners and returns Lead, Advertising, SEO, Security, and overall values from 0 to 100. Future versions must persist a score algorithm version with each audit/report so historical reports remain stable.
