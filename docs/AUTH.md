# Authentication

Argon2id password hashing, short-lived JWT access-token construction, random refresh tokens stored only as hashes, and session revocation fields are established. Registration/login/logout route services and email verification, reset, OAuth, passkey, and 2FA adapters are Phase 1 work.
